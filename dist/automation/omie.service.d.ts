export declare class OmieService {
    private readonly appKey;
    private readonly appSecret;
    private readonly baseUrl;
    listarTodosClientes(): Promise<any[]>;
    incluirContaIndividual(conta: any): Promise<any>;
}
